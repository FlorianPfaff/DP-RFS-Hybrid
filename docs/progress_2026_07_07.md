# Progress 2026-07-07

Implemented during the first major development pass:

- added configurable DP clutter responsibility learning gates to `LabeledMultiBernoulliTracker`;
- added tests for attenuated and gated clutter learning;
- added delayed DP birth learning with confirmation age/existence thresholds;
- added tests showing birth atoms are not updated until a tentative birth is confirmed;
- added `experiments/structured_clutter_metrics.py` for multi-seed CSV output;
- added a structured-clutter plotting script;
- added a fixed Gaussian-mixture clutter baseline;
- documented responsibility controls and structured-clutter metrics;
- added a paper-side TODO map.

Follow-up progress after CI:

- hosted GitHub Actions workflow is green on Python 3.10, 3.11, and 3.12;
- stale bootstrap issues were closed;
- paper notes now include the DP component-count caveat;
- DP birth now separates normalized posterior-predictive birth density from the scalar `birth_rate` scale.

Remaining high-value items:

- reclustering for confirmed birth evidence;
- fixed Gaussian-mixture baseline in the reusable experiment helper;
- harder nonstationary structured-clutter scenarios;
- PMBM/GLMB posterior-predictive insertion interface sketch as code.
